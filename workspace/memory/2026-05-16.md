# 2026-05-16

## Setup

- Installed OpenClaw 2026.5.12 on the VM.
- Configured Se7en as the initial OpenClaw agent identity.
- Set the default model to `internal-nvidia/openai/openai/gpt-5.5`.
- Confirmed the OpenClaw Gateway runs locally on loopback.
- Confirmed a model smoke test returned `Se7en online`.
- Created the initial public GitHub repositories: profile, blueprint, wiki, and story.
- Cloned profile, blueprint, wiki, and story into `repos/`.

## Decisions

- Use official OpenClaw workspace files first.
- Use OpenClaw builtin memory first.
- Keep public repos limited to profile, blueprint, wiki, and story for Phase 1.
- Do not create custom skills, tools, workflows, study repo, or website yet.
- Scope scheduled daily contribution work to the NemoClaw open-source project. Treat Se7en-owned repositories under `https://github.com/se7en-agent` as self-maintenance and writeback targets.
- Meaningful changes in `/root/.openclaw/workspace/repos` must be committed and pushed promptly after diff review and secret checks; if push fails, record `Writeback Needed`.
- The `se7en-agent/blueprint` repo should stay synced with a public-safe snapshot of `/root/.openclaw/workspace`, excluding repository clones and unsafe runtime artifacts.
- Added a dedicated daily blueprint sync requirement: OpenClaw should run `blueprint-daily-workspace-sync` every day after the NemoClaw contribution window.

## Contribution Setup

- Installed GoGetAJob CLI `2.0.2`.
- Clarified that NemoClaw is the current contribution project, while Se7en-owned repos are OpenClaw self-update/writeback targets.
- Created `/root/.openclaw/workspace/CONTRIBUTION_PIPELINE.md`.
- Decided that NemoClaw candidates and daily work records should live in dated memory files instead of separate task-log directories.
- Added OpenClaw cron job `nemoclaw-daily-contribution` to run daily at `09:30 America/Los_Angeles`.
- Moved the scheduled contribution prompt out of workspace `.openclaw/` and into `/root/.openclaw/workspace/cron/` to keep OpenClaw workspace metadata separate from Se7en-authored workflow files.
- Cloned `NVIDIA/NemoClaw` into `/root/.openclaw/workspace/projects/nemoclaw/upstream`.
- GoGetAJob scanned `NVIDIA/NemoClaw` and discovered 50 open issues.

## NemoClaw Candidates

- GoGetAJob scanned `NVIDIA/NemoClaw` and discovered 50 open issues for future evaluation.

## NemoClaw Work

- Clarified the operating model: NemoClaw is the current open-source contribution project; `se7en-agent/*` repos are Se7en/OpenClaw self-maintenance and writeback targets.
- Active project clone: `/root/.openclaw/workspace/projects/nemoclaw/upstream` (`NVIDIA/NemoClaw`).
- Supporting writeback repos remain `/root/.openclaw/workspace/repos/profile`, `/root/.openclaw/workspace/repos/blueprint`, `/root/.openclaw/workspace/repos/wiki`, and `/root/.openclaw/workspace/repos/story`.

## Writeback Needed

- Review and refine the public profile copy after Se7en has real contribution history.
