## Operating Instruction - NemoClaw Pull Before Fixes

- cr7258 instructed Se7en to pull the latest NemoClaw code every time before starting a fix.
- Updated `MEMORY.md`, `CONTRIBUTION_PIPELINE.md`, and `cron/nemoclaw-daily-contribution.md` to make this explicit: fetch/pull `NVIDIA/NemoClaw` from `origin/main` when clean; preserve local changes and record a blocker instead of overwriting them.

## Story Journal Structure

- Added `journal/` to `se7en-agent/story` after cr7258 pointed to Kagura's daily story journal as the desired style.
- Created `journal/README.md`, `journal/TEMPLATE.md`, and the first daily entry `journal/2026-05-17.md`.
- Updated the story README to describe the daily journal as part of the repository structure.
- Committed and pushed to `se7en-agent/story` as `53af0b1` (`doc: add daily journal structure`).

## Runtime Config - Compaction Reserve Floor

- Updated local OpenClaw config `agents.defaults.compaction.reserveTokensFloor` from unset/default to `200000` using `openclaw config set` after cr7258 requested the change.
- `openclaw config validate` passed. Gateway restart is required for the setting to apply.

## Story Repository Reorganization

- cr7258 asked to remove `retrospectives/` and `retrospetives/`, add yesterday's journal entry, and organize contributions by date.
- Updated `se7en-agent/story`: removed `retrospectives/`, added `journal/2026-05-16.md`, renamed contribution record from `contributions/2026-05.md` to `contributions/2026-05-16.md`, and added `contributions/README.md` documenting `contributions/YYYY-MM-DD.md`.
- Committed and pushed to `se7en-agent/story` as `daacad7` (`doc: organize story by dated entries`).

## Story Dispatches Removal

- cr7258 asked to remove the `dispatches/` directory from `se7en-agent/story`.
- Removed `dispatches/README.md` and updated the story README so the repository structure is now only `journal/` and `contributions/`.
- Committed and pushed to `se7en-agent/story` as `c69d4ff` (`doc: remove dispatches section`).

## Story Journal-Only Structure

- cr7258 asked to remove `contributions/` too and keep all story writing in `journal/`.
- Updated `se7en-agent/story`: removed `contributions/`, rewrote the README so the only structure is `journal/YYYY-MM-DD.md`, and expanded both `journal/2026-05-16.md` and `journal/2026-05-17.md`.
- Committed and pushed to `se7en-agent/story` as `7f93524` (`doc: keep story as journal only`).

## NemoClaw #2021 Slash Command Follow-up

- cr7258 asked Se7en to continue fixing NVIDIA/NemoClaw#2021 and configure uv so local validation is real.
- Pulled `NVIDIA/NemoClaw` latest `origin/main`; branch `fix/2021-nemoclaw-slash-command` was already up to date.
- Configured docs Python dependencies with `uv sync --group docs`; verified `uv run --group docs sphinx-build --version` returns `sphinx-build 7.4.7`.
- Found NemoClaw plugin registration exists, but Docker image plugin install was fail-open: `(openclaw plugins install /opt/nemoclaw > /dev/null 2>&1 || true)` could hide OpenClaw install blocking and leave `/nemoclaw` unavailable.
- Changed `Dockerfile` to fail closed and install the local plugin with `--dangerously-force-unsafe-install`, because OpenClaw 2026.5.12 blocks the plugin on `child_process` dangerous-code detection unless this explicit override is used.
- Tightened `test/e2e-test.sh` so plugin install failure fails the e2e smoke instead of passing when `dist/index.js` exists.
- Added `test/sandbox-provisioning.test.ts` regression checks for strict plugin install behavior.
- Local verification passed: temp-state `openclaw plugins install ./nemoclaw --dangerously-force-unsafe-install`; `npm test -- --run test/sandbox-provisioning.test.ts`; targeted vitest for plugin install/schema/CLI marker; `npm run build:cli`; `npm run lint -- --files Dockerfile test/e2e-test.sh test/sandbox-provisioning.test.ts` (with one unrelated existing Biome warning); `npm run typecheck:cli`; `git diff --check`; diff secret grep.
- Committed upstream branch change locally as `c17a136ae fix: fail closed when NemoClaw plugin install fails`.
- Investigation also found a likely remaining OpenClaw-side issue: OpenClaw 2026.5.12 TUI autocomplete builds from core chat commands and may not merge plugin command registry entries, so `/nemoclaw` autocomplete may still require an upstream OpenClaw fix after NemoClaw guarantees plugin installation.
